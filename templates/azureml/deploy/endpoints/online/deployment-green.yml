$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineDeployment.schema.json

name: green
endpoint_name: {{ENDPOINT_NAME}}

# Bump model version here when promoting a new model
model: azureml:{{MODEL_NAME}}:{{MODEL_VERSION}}

environment: azureml:inference-env:{{INFERENCE_ENV_VERSION}}

code_configuration:
  code: ../../../ml/components/train
  scoring_script: score.py

instance_type: {{INSTANCE_TYPE}}
instance_count: 1                      # lower while canary — scale up after promotion

liveness_probe:
  initial_delay: 10
  period: 10
  timeout: 2
  failure_threshold: 30

readiness_probe:
  initial_delay: 10
  period: 10
  timeout: 2
  failure_threshold: 10

request_settings:
  max_concurrent_requests_per_instance: 5
  request_timeout_ms: 5000

scale_settings:
  type: default